<?xml version="1.0" encoding="utf-8"?>
<!--Layout resource for the item chooser dialog activity.-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/list_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    tools:context="com.tlongdev.bktf.ItemChooserActivity">

    <!--Spinner for switching between normal and unusual items-->
    <Spinner
        android:id="@+id/spinner_item"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <!--Spinner for choosing an effect-->
    <Spinner
        android:id="@+id/spinner_effect"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clickable="false"
        android:paddingBottom="8dp"
        android:paddingTop="8dp" />

    <!--Listview containing the matches-->
    <ListView
        android:id="@+id/list_view_item_chooser"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scrollbars="none" />

    <!--Shows if there is not match-->
    <TextView
        android:id="@+id/text_view_no_match"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:paddingBottom="8dp"
        android:paddingTop="8dp"
        android:text="@string/item_chooser_no_match"
        android:textColor="@color/text_view_default" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <!--Input for searching-->
        <EditText
            android:id="@+id/edit_text_search"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="3"
            android:hint="@string/item_chooser_hint"
            android:inputType="text"
            android:textColorHighlight="@color/primary_light"
            android:textSize="16sp" />

        <!--Number of items to be added-->
        <EditText
            android:id="@+id/edit_text_count"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:inputType="number"
            android:text="@string/item_chooser_count"
            android:textColorHighlight="@color/primary_light"
            android:textSize="16sp" />

    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="end"
        android:orientation="horizontal">

        <!--Cancel button-->
        <Button
            android:id="@+id/button_cancel"
            style="?android:attr/buttonBarButtonStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/action_cancel"
            android:textColor="@color/primary" />

        <!--Add button-->
        <Button
            android:id="@+id/button_add"
            style="?android:attr/buttonBarButtonStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:enabled="false"
            android:text="@string/action_add"
            android:textColor="@color/primary" />

    </LinearLayout>

</LinearLayout>
